# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.env

# OS
.DS_Store

# 수동 provider 작업 폴더 (프롬프트 덤프·붙여넣기 응답 — 실행 기록만 커밋)
docs/eval/runs/manual/

# 문서별 추출 캐시 (원문 해시 키 — 재실행 비용 절감용, 실행 기록만 커밋)
docs/eval/runs/perdoc_cache/

# 빌드 산출 (CI 에서 생성)
/site/

# node 로컬 설치 (덱 생성 때만 쓰는 pptxgenjs — 저장소는 담지 않는다)
/node_modules/

# 제출 산출물 (생성기로 재현한다 — tools/build_deck.js · tools/record_demo.py)
/*.pptx
/*.webm
/*.timeline.txt
